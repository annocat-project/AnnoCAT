# Portable configuration

User configuration and saved profiles live here. Runtime contents are intentionally ignored by Git.

`fastvep-pin.json` records the exact upstream source and dependency-lock identity
approved for AnnoCat builds. It is not an executable checksum; packaged binaries
must be hashed and verified separately.
